<html>
<head>
  <title> Список завдань </title>
</head>
<body>
  <div>
    <h1> Список завдань </h1>
    <form action="/addnewtask" method="POST">
      <input type="text" name="newtask" placeholder="Введіть нове завдання">
      <button type="submit" title="Для додавання введеного нового завдання клацніть на цій кнопці"> Додати завдання </button>
      <h2> Додані завдання </h2>
      <ul>
        <% for( var i=0; i < task.length; i++){ %>
          <li><input type="checkbox" name="check" value="<%= task[i] %>" />
            <%= task[i] %>
          </li>
          <% } %>
      </ul>
      <button formaction="/movetocompletetask" type="submit" title="Для виконання відмічених завдань клацніть на цій кнопці"> Виконати завдання </button>
      <h2> Виконані завдання </h2>
      <ul>
        <% for(var i=0; i < complete.length; i++){ %>
          <li><input type="checkbox" checked>
            <%= complete[i] %>
          </li>
          <% } %>
      </ul>
    </form>
  </div>
</body>
</html>
